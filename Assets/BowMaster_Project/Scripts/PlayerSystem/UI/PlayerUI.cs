using UnityEngine;
using TMPro;

namespace PlayerSystem
{
    public class PlayerUI : MonoBehaviour
    {
        private GameObject displayHolder;
        private TextMeshProUGUI powerText, angleText;

        // Use this for initialization
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}