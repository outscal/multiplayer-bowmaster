using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace WeaponSystem
{
    public class WeaponFireView : WeaponView
    {
        public override void Shoot(float force, Vector2 direction)
        {
            base.Shoot(force, direction);
        }
    }
}